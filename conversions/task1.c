// // wap to ask the user to enter name ,city and state and then display that to the user
// #include<stdio.h>
// int main()
// {
// 	char name[20],city[10],state[10];
// 	puts("Enter name : ");
// 	gets(name);
// 	puts(name);
// 	puts("Enter city : ");
// 	gets(city);
// 	puts(city);
// 	puts("Enter state : ");
// 	gets(state);
// 	puts(state);
// 	return 0;	
// }
// wap to ask teh user to enter roll no, reg no,name,city,state,cgpa and then display all the details to user
#include<stdio.h>
int main()
{
	char rollno,regno,name,city,state,cgpa;
	puts("Enter roll no: ");
	gets(rollno);
	puts(rollno);
	puts("Enter regno : ");
	gets(regno);
	puts(regno);
	puts("Enter name : ");
	gets(name);
	puts(name);
    puts("Enter city: ");
    gets(city);
    puts(city);
    puts("enter state: ");
    gets(state);
    puts(state);
    puts("Enter cgpa: ");
    gets(cgpa);
    puts(cgpa);
	return 0;
}